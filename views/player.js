import {card,kpi} from '../components/card.js';import {table} from '../components/table.js';import {lineChart,bars} from '../js/charts.js';import {fmt,pct} from '../js/utils.js';
export function player(c,name){const p=c.players[decodeURIComponent(name)]||c.ranking[0];if(!p)return'<h1>Jogador não encontrado</h1>';const rows=p.history.slice().reverse();return`<div class="page-head"><h1>${p.name}</h1><span class="pill">Rank #${p.rank}</span></div><div class="grid cards">${kpi('MMR',fmt(p.finalMMR))}${kpi('Vitórias',p.wins)}${kpi('Derrotas',p.losses)}${kpi('Winrate',pct(p.winrate))}</div><div class="grid two"><section class="card"><h3>MMR</h3>${lineChart([{name:p.name,values:p.mmrHistory}])}</section>${card('Funções',bars(Object.entries(p.roles).map(([label,v])=>({label,value:v.matches}))))}${card('Parceiros',bars(Object.entries(p.teammates).sort((a,b)=>b[1].matches-a[1].matches).slice(0,10).map(([label,v])=>({label,value:v.matches})))}${card('Rivais',bars(Object.entries(p.opponents).sort((a,b)=>b[1].matches-a[1].matches).slice(0,10).map(([label,v])=>({label,value:v.matches})))}</div><h2>Histórico</h2>${table([{key:'match',label:'Partida'},{key:'result',label:'Resultado'},{key:'role',label:'Função'},{key:'delta',label:'MMR'},{key:'mmr',label:'MMR após'}],rows)}`}
