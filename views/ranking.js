import {table} from '../components/table.js';import {fmt,pct} from '../js/utils.js';import {searchBox} from '../components/search.js';
export function ranking(c){const rows=c.ranking;return`<div class="page-head"><h1>Ranking</h1>${searchBox('Buscar jogador')}</div>${table([{key:'rank',label:'#'},{key:'name',label:'Jogador',render:r=>`<a class="gold" href="#/player/${encodeURIComponent(r.name)}">${r.name}</a>`},{key:'finalMMR',label:'MMR',render:r=>fmt(r.finalMMR)},{key:'wins',label:'Vitórias'},{key:'losses',label:'Derrotas'},{key:'winrate',label:'Winrate',render:r=>pct(r.winrate)}],rows)}`}
