export function lineChart(series){const w=900,h=280,p=24,vals=series.flatMap(s=>s.values);const min=Math.min(...vals,0),max=Math.max(...vals,1);const sx=i=>p+i*((w-p*2)/Math.max(1,Math.max(...series.map(s=>s.values.length))-1));const sy=v=>h-p-((v-min)/(max-min||1))*(h-p*2);return`<svg class="chart" viewBox="0 0 ${w} ${h}" role="img">${series.map((s,si)=>`<polyline fill="none" stroke="${['#f0d084','#45d483','#1e90ff','#e05252','#b76eff'][si%5]}" stroke-width="3" points="${s.values.map((v,i)=>`${sx(i)},${sy(v)}`).join(' ')}"/><text x="${p}" y="${18+si*18}" fill="currentColor">${s.name}</text>`).join('')}</svg>`}
export function bars(items){const max=Math.max(...items.map(i=>i.value),1);return items.map(i=>`<p><span>${i.label}</span> <b>${i.value}</b></p><div class="bar"><span style="width:${i.value/max*100}%"></span></div>`).join('')}
